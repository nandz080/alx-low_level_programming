#include "main.h"
#include <stdio.h>

/**
 * main - prints a string followed bynew line to stdout.
 *
 * Return: Always 0.
*/

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
