#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'prints the alphabet in lowercase, followed by a new line'
 *
 * Return: Always 0 (Success)
**/

int main(void)
{
	int ch = '1';

	for (ch = '1' ; ch <= '9'; ch++)
	putchar(ch);
	putchar('\n');
	return (0);
}
