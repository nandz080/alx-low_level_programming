#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'prints the alphabet in lowercase, followed by a new line'
 *
 * Return: Always 0 (Success)
**/

int main(void)
{
	int ch = '0';

	for (ch = '0' ; ch <= '9'; ch++)
	putchar(ch);
	putchar('\n');
	return (0);
}
