#include <stdlib.h>
#include <stdio.h>

/**
 *
 * main -function that prints its name
 *
 * return - returns 0 if successful
 *
 **/
int main(int argc, char* argv[])
{
	if (argc >= 0)

		printf("%s\n", argv[0]);

	return (0);
}
